/**
 * PPDO Portal — TypeScript type barrel
 *
 * Add per-feature type files alongside this barrel:
 *   src/types/auth.ts         — LoginRequest, TokenResponse, MeResponse
 *   src/types/purchase-request.ts — CreatePRRequest, PRResponse, PRItem, ...
 *   src/types/delivery.ts
 *   src/types/item.ts
 *   src/types/user.ts
 *
 * Types here must mirror the backend DTOs in PPDO.Application/DTOs/.
 *
 * TODO RAL-xx: Populate as each feature is implemented.
 */

export {};
