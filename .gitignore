# ============================================================
# PPDO Portal — .gitignore
# Covers: .NET 9, Azure Functions, Next.js, Node.js
# ============================================================

# ── SECRETS — NEVER COMMIT ──────────────────────────────────
backend/PPDO.Functions/local.settings.json
frontend/.env.local
.env.local
.env.*.local

# ── .NET / C# ────────────────────────────────────────────────
[Bb]in/
[Oo]bj/
[Ll]og/
[Ll]ogs/
*.user
*.suo
*.userosscache
*.sln.docstates
.vs/
*.rsuser
*_i.c
*_p.c
*.ilk
*.meta
*.obj
*.iobj
*.pch
*.pdb
*.ipdb
*.pgc
*.pgd
*.rsp
*.sbr
*.tlb
*.tli
*.tlh
*.tmp
*.tmp_proj
*_wpftmp.csproj
*.vspscc
*.vssscc
.builds
*.pidb
*.svclog
*.scc
_ReSharper*/
*.[Rr]e[Ss]harper
*.DotSettings.user
.idea/
$tf/
*.Cache
ClientBin/
~$*
*.dbmdl
*.dbproj.schemaview
*.jfm
*.pfx
*.publishsettings
orleans.codegen.cs
Generated_Code/
_UpgradeReport_Files/
Backup*/
UpgradeLog*.XML
UpgradeLog*.htm
ServiceFabricBackup/
*.rptproj.bak
*.mdf
*.ldf
*.ndf
*.rdl.data
*.bim.layout
*.bim_*.settings
*.rptproj.rsuser
*- [Bb]ackup.rdl
*- [Bb]ackup ([0-9]).rdl
*- [Bb]ackup ([0-9][0-9]).rdl
*.GhostDoc.xml
.ncrunchsolution
*.ncrunchproject
*.covrunsettings
TestResult.xml
[Dd]ebugPS/
[Rr]eleasePS/
dlldata.c
BenchmarkDotNet.Artifacts/
project.lock.json
project.fragment.lock.json
artifacts/
ScaffoldingReadMe.txt
StyleCopReport.xml
*.[Cc]ache
!?*.[Cc]ache/
PackageScripts
node_modules/
bower_components/
*.VisualState.xml
TestResults/
[Dd]eploy/
.localhistory/
.vshistory/
healthchecksdb
MigrationBackup/
ipch/
.vs/
xcuserdata/

# ── Azure Functions ──────────────────────────────────────────
backend/PPDO.Functions/local.settings.json
.python_packages/
bin/
obj/
.funcignore

# ── Entity Framework ─────────────────────────────────────────
# Keep Migrations folder — never ignore it
# *.mdf and *.ldf already covered above

# ── Next.js / Node.js ────────────────────────────────────────
node_modules/
.next/
out/
.cache/
.parcel-cache/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz
.pnp.*

# ── Environment files ────────────────────────────────────────
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
# Keep example files — they are safe to commit
!.env.example
!local.settings.json.example

# ── OS files ─────────────────────────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# ── Editor files ─────────────────────────────────────────────
.vscode/settings.json
.vscode/launch.json
.vscode/tasks.json
# Keep shared VS Code extension recommendations
!.vscode/extensions.json
*.swp
*.swo
*~

# ── Test output ──────────────────────────────────────────────
TestResults/
coverage/
*.coverage
*.coveragexml
lcov.info

# ── Build artifacts ──────────────────────────────────────────
publish/
dist/
build/
# Linear archive script progress log (local run state)
scripts/.linear_archive_log.tsv

# Python bytecode from the repo's scripts/ tooling
__pycache__/
*.py[cod]

# Manual filename-versioned copies of the tracker workbooks. The live file is
# tracked (docs/v1.8/v1.8.0_Open_Items_Tracker.xlsx); the ".backupN-DATE-before-X"
# copies are local working state and are not diffable by git.
# Retrospective action item 9 — docs/v1.8/RETROSPECTIVE.md §3.7.
docs/**/*.backup*.xlsx

# Database exports and backups. This repository is PUBLIC and production carries real
# personnel names and budget figures — a .bacpac or .bak committed here publishes them.
# Keep rehearsal copies outside the repo entirely; these patterns are the safety net,
# not the plan. See docs/v1.8/Units_Migration_Runbook.md §1a.
*.bacpac
*.bak
*.dacpac
*.mdf
*.ldf
