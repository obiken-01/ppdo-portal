---
id: RAL-167
title: 'Dedupe /auth/me calls on AIP detail/new/import-preview pages (frontend)'
status: Done
project: PPDO Portal
labels: v1.4
milestone: v1.4.5 — Budget Planning Dashboard Rework
created: 2026-07-17
completed: 2026-07-17
parent: RAL-116
blocked_by: (none)
related: RAL-90
url: https://linear.app/ralphoksiprojects/issue/RAL-167
uuid: 58d201ad-c0c3-41ab-8a8c-e106ad0fe85e
---

# RAL-167 — Dedupe /auth/me calls on AIP detail/new/import-preview pages (frontend)

> 🌿 Working branch: `release/1.4.5`. PR targets `release/1.4.5` (NOT `main`). Child of epic [RAL-116](<https://linear.app/ralphoksiprojects/issue/RAL-116/wfp-rework-epic-v14>).
> 📌 Frontend-only, small. **Tier 1, item 5** of the performance audit.

## Why

`docs/Performance_Audit_2026-07-16.md` §2.1 found `budget-planning/aip/detail/page.tsx:152`, `aip/new/page.tsx:36`, and `aip/import-preview/page.tsx:66` all call `api.get("/auth/me")` directly, bypassing the shared `useMe()`/`fetchMe()` cache already mounted once per portal in `layout.tsx:121` — this is the exact [RAL-90](https://linear.app/ralphoksiprojects/issue/RAL-90/perf-deduplicate-authme-shared-current-user-context) bug, reintroduced page-by-page, and the exact rule CLAUDE.md states ("Fetch shared state once").

## Files to read before writing code

* `docs/Performance_Audit_2026-07-16.md` §2.1
* CLAUDE.md — "Fetch shared state once" rule
* `frontend/src/app/(portal)/layout.tsx:121` (the one `fetchMe()` call) and wherever `useMe()` is defined/exported
* `frontend/src/app/(portal)/budget-planning/wfp/page.tsx` or `aip/page.tsx` — confirmed-correct `useMe()` usage per the audit; copy this pattern exactly
* `frontend/src/app/(portal)/budget-planning/aip/detail/page.tsx:152`
* `frontend/src/app/(portal)/budget-planning/aip/new/page.tsx:36`
* `frontend/src/app/(portal)/budget-planning/aip/import-preview/page.tsx:66`

## Working branch

`release/1.4.5`. Create `fix/v1.4.5-ral-167-aip-dedupe-auth-me` off `release/1.4.5`, PR against `release/1.4.5` (NOT `main`).

No TDD — frontend data-fetching change, no service logic.

## Steps

1. Replace each direct `api.get("/auth/me")` call with a read from the shared `useMe()` hook/context.
2. Confirm no behavioral difference (same user object shape used downstream on each page).
3. Verify via the Browser pane's Network tab: exactly one `/auth/me` request per portal mount, across navigation through all 3 pages.

## Out of scope

Don't touch `dashboard/page.tsx`, `admin/users/page.tsx`, `announcements/page.tsx`, `resource-links/page.tsx` (Tier 2 scope) or `inventory/pr-report/page.tsx` (Tier 3 scope) — this ticket is AIP pages only.

## Commit message

`fix(budget-planning): dedupe /auth/me calls on AIP detail/new/import-preview pages via shared useMe() (RAL-167)`
