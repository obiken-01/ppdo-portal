---
id: RAL-164
title: 'ProcurementPresetService: scope price_index_items lookup instead of full scan on preset save (backend)'
status: Done
project: PPDO Portal
labels: v1.4
milestone: v1.4.5 — Budget Planning Dashboard Rework
created: 2026-07-17
completed: 2026-07-17
parent: RAL-116
blocked_by: (none)
related: RAL-170
url: https://linear.app/ralphoksiprojects/issue/RAL-164
uuid: 91b36d8a-9908-49f7-bdb7-a1d8f291d8ce
---

# RAL-164 — ProcurementPresetService: scope price_index_items lookup instead of full scan on preset save (backend)

> 🌿 Working branch: `release/1.4.5`. PR targets `release/1.4.5` (NOT `main`). Child of epic [RAL-116](<https://linear.app/ralphoksiprojects/issue/RAL-116/wfp-rework-epic-v14>).
> 📌 Backend-only, small. **Tier 1, item 2** of the performance audit.

## Why

`docs/Performance_Audit_2026-07-16.md` §1 found `ProcurementPresetService.cs:183, 213` full-scans `price_index_items` on every preset save. This table (the GSO catalogue) can run to hundreds/thousands of rows, and it directly feeds WFP procurement line items.

## Files to read before writing code

* `docs/Performance_Audit_2026-07-16.md` §1
* `docs/PERFORMANCE_GUIDELINES.md`
* `backend/PPDO.Application/Services/ProcurementPresetService.cs:183, 213`
* The price-index-item repository — existing lookup method shapes to extend
* `backend/PPDO.Tests/Application/ProcurementPresetServiceTests.cs`

## Working branch

`release/1.4.5`. Create `fix/v1.4.5-ral-164-procurement-preset-scoped-lookup` off `release/1.4.5`, PR against `release/1.4.5` (NOT `main`).

## TDD

Extend `ProcurementPresetServiceTests` with failing tests first, then implement.

## Steps

1. At each of the two line numbers, confirm exactly what the full scan is resolving (uniqueness check vs. id lookup vs. something else) before choosing the replacement query shape.
2. Replace with a scoped `WHERE`/`AnyAsync`/single-id query; add the repo method if missing.
3. Update tests; verify preset save behavior is unchanged for existing presets.

## Out of scope

Don't change preset save validation rules or the preset data model — query shape only.

## Commit message

`perf(budget-planning): scope ProcurementPresetService price-index lookup on preset save (RAL-164)`
