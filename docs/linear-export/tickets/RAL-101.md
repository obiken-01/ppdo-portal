---
id: RAL-101
title: 'Allocation page UI — ceiling, division allocation, PPA assignment'
status: Done
project: PPDO Portal
labels: frontend
milestone: v1.2 — Budget Allocation & Configurable Divisions
created: 2026-06-24
completed: 2026-06-29
parent: (none)
blocked_by: (none)
related: RAL-99, RAL-100, RAL-98
url: https://linear.app/ralphoksiprojects/issue/RAL-101
uuid: c0ff16ce-938e-4a1a-927e-ffb2a3eb6781
---

# RAL-101 — Allocation page UI — ceiling, division allocation, PPA assignment

The finance-officer Allocation page at `/budget-planning/allocation` (between AIP and WFP). Two tabs: ceiling + division allocation (with stacked bar), and PPA→division assignment. Depends on [RAL-99](https://linear.app/ralphoksiprojects/issue/RAL-99/allocation-backend-ceiling-division-allocation-ppa-assignment) (backend), [RAL-98](https://linear.app/ralphoksiprojects/issue/RAL-98/division-config-page-crud-csv-upsertexport) (divisions seeded), [RAL-100](https://linear.app/ralphoksiprojects/issue/RAL-100/shared-moneyinput-component-peso-prefix-comma-formatting) (MoneyInput).

> 🌿 **Working branch:** create `feature/v1.2-ral-101-allocation-ui` off `release/1.2.0` — PR targets `release/1.2.0` (**NOT main**).

---

### Implementation prompt

```
Read CLAUDE.md, PROJECT_DOCUMENTATION_NET_AZURE.md, and PPDO_PROJECT_CONTEXT.md.
Read docs/v1.2/Allocation_Requirements.md §3 FULLY — authoritative for the two tabs, the stacked bar,
and the PPA→division grid behavior.

Read these files before writing code:
- frontend/src/app/(portal)/budget-planning/wfp/page.tsx (the Sector→Program→… grid + collapse pattern to reuse/collapse to Sector→Program)
- frontend/src/components/ui/Modal.tsx, DataTable.tsx, Toast.tsx (reuse)
- frontend/src/components/ui/MoneyInput.tsx (from RAL-100 — use for all money fields)
- frontend/src/lib/wfp.ts + lib/aip.ts (client patterns for the AIP hierarchy + blob/JWT calls)
- frontend/src/lib/me-cache.ts (useMe; gate on canManageAllocation)
- frontend/src/components/layout/Sidebar.tsx (add "Allocation" BETWEEN AIP and WFP)

Working branch: release/1.2.0.
Create feature/v1.2-ral-101-allocation-ui off release/1.2.0 and open the PR against release/1.2.0 (NOT main).

1. Route /budget-planning/allocation gated by canManageAllocation (hidden in sidebar otherwise);
   add the sidebar link between AIP and WFP. FY + office selectors (office locked for non-PPDO finance).
2. Tab 1 — Ceiling & Division Allocation: ceiling MoneyInput; one MoneyInput per division; live
   "Allocated ₱X of ₱Y · Remaining ₱Z"; block save + red when over ceiling; stacked horizontal bar
   (segments per division + grey remainder + red overflow), % of ceiling per division.
3. Tab 2 — PPA→Division: reuse the WFP hierarchy collapsed to Sector→Program; columns = ref code,
   program name, "Multi-division?" toggle (OFF = radio/one division, ON = multi), one checkbox column
   per division; "Unassigned" filter/badge + bulk-assign + per-division assigned counts in headers.
4. lib/allocation.ts client + types; wire to the RAL-99 endpoints.

Do NOT introduce a new money-formatting helper — use MoneyInput / formatMoney from RAL-100. Flat design
(no rounded corners); reuse components/ui. Keep list/grid payloads slim (PERFORMANCE_GUIDELINES).

When done, commit with:
feat(budget-planning): allocation page — ceiling, division allocation, PPA assignment UI (RAL-101)
```

---

### Test plan

- [ ] Page hidden from sidebar + blocked for users without canManageAllocation.
- [ ] Tab 1: allocations can't exceed ceiling (live + on save); stacked bar reflects distribution + overflow.
- [ ] Tab 2: single-division (radio) vs multi-division toggle; assignments persist; unassigned filter + bulk-assign work.
- [ ] All money fields use MoneyInput; non-PPDO finance locked to own office.
- [ ] tsc + lint + build pass.

**Blocked by: **[RAL-98](https://linear.app/ralphoksiprojects/issue/RAL-98/division-config-page-crud-csv-upsertexport), [RAL-99](https://linear.app/ralphoksiprojects/issue/RAL-99/allocation-backend-ceiling-division-allocation-ppa-assignment), [RAL-100](https://linear.app/ralphoksiprojects/issue/RAL-100/shared-moneyinput-component-peso-prefix-comma-formatting).
